#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i=0; i<n; i++)
using ll = long long;

int main() {
    int k;
    cin >> k;
    string s, t;
    cin >> s >> t;
    string ss = s, tt = t;
    int ns = s.size(), nt = t.size();
    swap(t,s);
    cout << s << ' ' << t << endl;
    
return 0;
}