#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i=0; i<n; i++)
using ll = long long;

int main() {
    string s;
    cin >> s;
    int q;
    cin >> q;
    rep(i,q) {
        ll k;
        cin >> k;
        k--;
        ll n = k/s.size() + 1;
        int sn = k%s.size();
        
    }
return 0;
}