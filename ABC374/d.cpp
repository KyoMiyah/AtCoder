#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i=0; i<n; i++)
using ll = long long;

int main() {
    int n, s, t;
    cin >> n >> s >> t;
    vector<vector<int>> a(n, vector<int> (4));
    vector<bool> hantei(n);
    rep(i,n) cin >> a[i][0] >> a[i][1] >> a[i][2] >> a[i][3];

    while(n>0) {
        int nn = 2*n;
        
    } 
return 0;
}